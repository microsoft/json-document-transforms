namespace Microsoft.VisualStudio.Jdt
{
    /// <summary>
    /// Available default transforms for objects and arrays.
    /// </summary>
    public enum JdtDefaultTransform
    {
        /// <summary>
        /// Perform merge when doing the default transform.
        /// </summary>
        Merge,

        /// <summary>
        /// Perform replace when doing the default transform.
        /// </summary>
        Replace,
    }
}
